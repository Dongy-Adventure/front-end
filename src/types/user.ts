export type Buyer = {
  buyerId: string;
  name: string;
  surname: string;
};

export type Seller = {
  sellerId: string;
  name: string;
  surname: string;
  paymentInfo: string;
  role: string;
};
